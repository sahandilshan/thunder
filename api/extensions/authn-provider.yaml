openapi: 3.0.0
info:
  title: Authentication Provider API
  version: 1.0.0
  description: API specification for the Authentication Provider interface.

security:
  - ApiKeyAuth: []
paths:
  /authenticate:
    post:
      summary: Authenticate a user
      operationId: authenticate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthenticateRequest'
      responses:
        '200':
          description: Authentication successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthnResult'
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthnProviderError'
        '401':
          description: Authentication failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthnProviderError'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthnProviderError'

  /attributes:
    post:
      summary: Get user attributes
      operationId: getAttributes
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetAttributesRequest'
      responses:
        '200':
          description: Attributes retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetAttributesResult'
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthnProviderError'
        '401':
          description: Invalid token or unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthnProviderError'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthnProviderError'

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: API-KEY
  schemas:
    AuthenticateRequest:
      type: object
      properties:
        identifiers:
          type: object
          additionalProperties: true
          description: Map of user identifiers
        credentials:
          type: object
          additionalProperties: true
          description: Map of user credentials
        metadata:
          $ref: '#/components/schemas/AuthnMetadata'
      required:
        - identifiers
        - credentials

    GetAttributesRequest:
      type: object
      properties:
        token:
          type: string
          description: Authentication token
        requestedAttributes:
          $ref: '#/components/schemas/RequestedAttributes'
        metadata:
          $ref: '#/components/schemas/GetAttributesMetadata'
      required:
        - token

    AuthnMetadata:
      type: object
      properties:
        appMetadata:
          type: object
          additionalProperties: true
          description: Application metadata

    GetAttributesMetadata:
      type: object
      properties:
        appMetadata:
          type: object
          additionalProperties: true
          description: Application metadata
        locale:
          type: string
          description: User locale

    AuthnResult:
      type: object
      properties:
        userId:
          type: string
        userType:
          type: string
        ouId:
          type: string
        token:
          type: string
        availableAttributes:
          $ref: '#/components/schemas/AvailableAttributes'
      required:
        - userId
        - userType
        - ouId

    GetAttributesResult:
      type: object
      properties:
        userId:
          type: string
        userType:
          type: string
        ouId:
          type: string
        attributeResponse:
          $ref: '#/components/schemas/AttributesResponse'
      required:
        - userId
        - userType
        - ouId

    AvailableAttributes:
      type: object
      properties:
        attributes:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/AttributeMetadataResponse'
          description: Map of attribute names to their metadata
        verifications:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/VerificationResponse'
          description: Map of verification IDs to verification details

    AttributeMetadataResponse:
      type: object
      properties:
        assuranceMetadataResponse:
          $ref: '#/components/schemas/AssuranceMetadataResponse'

    AssuranceMetadataResponse:
      type: object
      properties:
        isVerified:
          type: boolean
          description: Whether the attribute is verified
        verificationId:
          type: string
          description: Key of the corresponding verification response in the verifications map

    VerificationResponse:
      type: object
      properties:
        trustFramework:
          type: string
          description: Trust framework used for verification
        time:
          type: string
          description: Time of verification
        verificationProcess:
          type: string
          description: Verification process used

    RequestedAttributes:
      type: object
      properties:
        attributes:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/AttributeMetadataRequest'
          description: Map of attribute names to their request metadata
        verifications:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/VerificationRequest'
          description: Map of verification IDs to verification requests

    AttributeMetadataRequest:
      type: object
      properties:
        genericMetadataRequest:
          $ref: '#/components/schemas/GenericMetadataRequest'
        assuranceMetadataRequest:
          $ref: '#/components/schemas/AssuranceMetadataRequest'

    GenericMetadataRequest:
      type: object
      properties:
        essential:
          type: boolean
          description: Whether this claim is essential
        value:
          type: string
          description: Specific value requested
        values:
          type: array
          items:
            type: string
          description: List of acceptable values

    GenericTimeMetadataRequest:
      allOf:
        - $ref: '#/components/schemas/GenericMetadataRequest'
        - type: object
          properties:
            maxAge:
              type: integer
              description: Maximum age in seconds

    AssuranceMetadataRequest:
      type: object
      properties:
        shouldVerify:
          type: boolean
          description: Whether verification is requested
        verificationId:
          type: string
          description: Key of the corresponding verification request in the verifications map

    VerificationRequest:
      type: object
      properties:
        trustFramework:
          $ref: '#/components/schemas/GenericMetadataRequest'
        verificationProcess:
          $ref: '#/components/schemas/GenericMetadataRequest'
        time:
          $ref: '#/components/schemas/GenericTimeMetadataRequest'

    AttributesResponse:
      type: object
      properties:
        attributes:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/AttributeResponse'
          description: Map of attribute names to their values and metadata
        verifications:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/VerificationResponse'
          description: Map of verification IDs to verification details

    AttributeResponse:
      type: object
      properties:
        value:
          description: The attribute value
        assuranceMetadataResponse:
          $ref: '#/components/schemas/AssuranceMetadataResponse'

    ErrorCode:
      type: string
      description: |
        Error codes returned by the authentication provider API:
        - AUP-0001: System Error
        - AUP-0002: Authentication Failed
        - AUP-0003: User Not Found
        - AUP-0004: Invalid Token
        - AUP-0005: Not Implemented
        - AUP-0006: Invalid Request
      enum:
        - AUP-0001
        - AUP-0002
        - AUP-0003
        - AUP-0004
        - AUP-0005
        - AUP-0006

    AuthnProviderError:
      type: object
      properties:
        code:
          $ref: '#/components/schemas/ErrorCode'
        message:
          $ref: '#/components/schemas/I18nMessage'
        description:
          $ref: '#/components/schemas/I18nMessage'
      required:
        - code
        - message

    I18nMessage:
      type: object
      description: Internationalized message with translation key and default value.
      required:
        - key
        - defaultValue
      properties:
        key:
          type: string
          description: Translation key for fetching localized message.
        defaultValue:
          type: string
          description: Default message in English (fallback).
